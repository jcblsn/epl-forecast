[project]
name = "epl-forecast"
version = "0.1.0"
description = "Live probabilistic Premier League forecasts and season simulation"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.2",
    "scipy>=1.18.1",
]

[project.scripts]
epl-forecast = "epl_forecast.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
